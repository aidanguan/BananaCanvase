services:
  banana-canvas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: banana-canvas-app
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - banana-network

networks:
  banana-network:
    driver: bridge
